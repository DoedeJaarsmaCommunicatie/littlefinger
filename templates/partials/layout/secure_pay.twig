<aside class="flex">
    {% for icon in secure_payment %}
        <img src="{{ theme.link }}/dist/images/payment_methods/{{ icon.getFileName }}" alt="betaal veilig met {{ icon.getFileName | replace({'.svg': ''}) }}" />
    {% endfor %}
</aside>
